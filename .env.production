# Variáveis de ambiente para PRODUÇÃO
# Estas são variáveis padrão usadas durante o build no Render
# Os valores reais serão sobrescritos pelas variáveis definidas no painel do Render

NODE_ENV=production
DATABASE_CLIENT=pg
DATABASE_URL=postgres://localhost/rkts_node_api
PORT=3000
